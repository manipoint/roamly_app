name: roamly_logging
description: Structured, redacting, and testable logging primitives for Roamly.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.12.1

resolution: workspace

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
