name: roamly_networking
description: HTTP and WebSocket transport infrastructure for Roamly.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.12.1

resolution: workspace

dependencies:
  dio: ^5.11.0
  roamly_core:
    path: ../roamly_core
  roamly_logging:
    path: ../roamly_logging

dev_dependencies:
  test: ^1.25.6
