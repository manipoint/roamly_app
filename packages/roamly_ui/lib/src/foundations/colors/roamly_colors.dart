import 'package:flutter/material.dart';

/// Raw colors from the Roamly brand palette.
///
/// Widgets should normally consume semantic colors through [ColorScheme]
/// instead of referencing these values directly.
abstract final class RoamlyColors {
  static const Color deepInk = Color(0xFF0B1220);
  static const Color indigo = Color(0xFF5B5CF6);
  static const Color blue = Color(0xFF2563EB);
  static const Color cyan = Color(0xFF22C3EE);
  static const Color teal = Color(0xFF2DD4BF);
  static const Color background = Color(0xFFF6F7FB);
  static const Color white = Color(0xFFFFFFFF);
}
