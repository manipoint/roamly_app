abstract final class HomeCachePolicy {
  static const Duration destinationCollection = Duration(minutes: 2);
  static const Duration destinationDetail = Duration(minutes: 10);
  static const Duration destinationPlaceDetail = Duration(minutes: 15);
}
