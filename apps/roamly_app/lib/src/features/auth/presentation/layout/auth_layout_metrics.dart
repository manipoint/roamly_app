/// Shared layout dimensions for authentication screens.
abstract final class AuthLayoutMetrics {
  /// Maximum width of authentication content on larger screens.
  static const double maxContentWidth = 480;

  /// Standard logo width used on authentication screens.
  static const double logoWidth = 180;
}
